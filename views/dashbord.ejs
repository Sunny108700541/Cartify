<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Add Product</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            text-align: center;
        }
        .container {
            width: 50%;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }
        input, select, textarea {
            width: 90%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        button {
            width: 95%;
            padding: 10px;
            background: green;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background: darkgreen;
        }
    </style>
</head>
<body>


    <div class="container">

        <div>
            <h2>DASHBOARD OVERVIEW</h2>
            <h3>Total Products</h3>
            <p><%= productcount %></p>
            <h3>Total Categories</h3>
            <p><%= categorycount %></p>
            
        </div>
        <h2>Add a New Product</h2>


        <form action="/product" method="POST" id="addProductForm"  >
            <button name="logout" id="lgt"><a href="/admin/logout">logout</a></button>
            <input type="text" name="name" placeholder="Product Name (min 2 - max 100 characters)" minlength="2" maxlength="100" required>
            <input type="number" name="price" placeholder="Product Price (min 0)" min="0" required>
            <input type="text" name="category" placeholder="Category (min 2 - max 50 characters)" minlength="2" maxlength="50" required>
            <select name="stock" required>
                <option value="">Select Stock Availability</option>
                <option value="true">In Stock</option>
                <option value="false">Out of Stock</option>
            </select>
            <textarea name="description" placeholder="Description (min 10 - max 1000 characters)" minlength="10" maxlength="1000" required></textarea>
            
            <!-- Updated Image Input -->
            <input type="text" name="image" placeholder="Enter Image URL" required>


            <button type="submit">Add Product</button>
        </form>

        <!--delete product from product id-->
        <form action="/product/delete" method="POST">
            <h3>Delete product</h3>
            <input type="text" name="product_id" placeholder="Product ID" required>
            <button type="submit">Delete Product</button>

        </form>


    </div>

</body>
</html>
